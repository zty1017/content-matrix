收到！我将严格遵循规格文档，为你提供 Content Matrix 前端 MVP 的底层架构实现。

本轮将输出：
1. **完整 TypeScript 类型定义** (`src/types/`)：保证前后端契约与前端 UI 状态的类型安全。
2. **Demo Fallback 数据** (`src/data/`)：保证离线或 API 断开时产品依旧能跑通完整流程。
3. **全局状态管理层** (`src/store/`)：完全对应规格中的 4 个 Zustand Store 设计，预先梳理好复杂的业务逻辑状态流。

---

### 1. TypeScript 类型定义 (`src/types/`)

#### `src/types/api.ts`
后端接口契约层映射。
```typescript
export type CubeState = "input_ready" | "transforming" | "ready" | "blocked" | "failed";

export type CubeAnimationPhase =
  | "source_resolution"
  | "content_reconstruction"
  | "matrix_linking"
  | "final_form"
  | "needs_confirmation"
  | "error";

export type CubeFaceId =
  | "source"
  | "primary_card"
  | "related_assets"
  | "inferences"
  | "evidence"
  | "snapshot";

export interface CubeFace {
  face_id: CubeFaceId;
  face_type: string;
  title: string;
  status: string;
  target_ref?: { type: string; id: string } | null;
  display_blocks: Array<Record<string, unknown>>;
  action?: Record<string, unknown> | null;
}

export interface CubeProgressStep {
  step_id: string;
  title: string;
  phase: CubeAnimationPhase;
  percent: number;
  status: string;
}

export interface CubeProgress {
  task_id: string;
  cube_state: CubeState;
  animation_phase: CubeAnimationPhase;
  percent: number;
  message: string;
  steps: CubeProgressStep[];
}

export interface CubeView {
  task_id: string;
  video_asset_id: string;
  cube_state: CubeState;
  animation_phase: CubeAnimationPhase;
  source_status: string;
  asset_status: string;
  retrieval_status: string;
  progress: CubeProgress;
  faces: CubeFace[];
  warnings: string[];
}
```

#### `src/types/cube.ts`
前端 UI 模型适配层，用于驱动 3D 渲染与右侧面板。
```typescript
import { CubeState, CubeAnimationPhase, CubeFaceId, CubeProgress } from "./api";

export type UiDetailBlockType =
  | "summary"
  | "key_points"
  | "actions"
  | "evidence"
  | "related_assets"
  | "inferences"
  | "source_preview"
  | "snapshot_form"
  | "custom";

export interface UiDetailBlock {
  type: UiDetailBlockType;
  title: string;
  items?: string[];
  data?: unknown;
}

export type UiFaceStatus = "empty" | "loading" | "ready" | "needs_confirmation" | "blocked" | "failed" | "disabled";
export type UiFaceAccent = "cyan" | "blue" | "violet" | "amber" | "red" | "neutral";

export interface UiCubeFace {
  id: CubeFaceId;
  label: string;
  subtitle?: string;
  status: UiFaceStatus;
  icon: string;
  accent: UiFaceAccent;
  summaryItems: string[];
  detailBlocks: UiDetailBlock[];
  isAiExtracted?: boolean;
  isManuallyEnhanced?: boolean;
  isConfirmed?: boolean;
  needsConfirmation?: boolean;
  highRisk?: boolean;
}

export interface UiCubeModel {
  taskId: string;
  assetId: string;
  title: string;
  state: CubeState;
  phase: CubeAnimationPhase;
  faces: Record<CubeFaceId, UiCubeFace>;
  progress: CubeProgress;
  referencedAssetIds: string[];
  warnings: string[];
}
```

#### `src/types/assets.ts`
资产陈列柜专用类型。
```typescript
import { UiCubeModel } from "./cube";

export type AssetStatus = "formal" | "draft" | "needs_confirmation";
export type AssetType = "knowledge" | "decision" | "experience" | "procedure" | "script";

export interface AssetCube {
  id: string;
  title: string;
  shortSummary: string;
  type: AssetType;
  status: AssetStatus;
  scenarioTags: string[];
  sourceType: "douyin" | "local_video" | "manual" | "demo_seed" | "fallback";
  cover?: string;
  cubeView?: UiCubeModel;
  usedCount?: number;
  lastUsedFor?: string;
  currentTaskReferenced?: boolean;
  manuallyConfirmed?: boolean;
  highRisk?: boolean;
  needsConfirmation?: boolean;
  createdAt?: string;
}

export type CabinetShelfKind = "current_related" | "recent" | "frequent" | "draft" | "type" | "scenario" | "search";

export interface CabinetShelf {
  id: string;
  title: string;
  description?: string;
  assetIds: string[];
  kind: CabinetShelfKind;
}
```

#### `src/types/workshop.ts`
魔方工坊类型定义。
```typescript
export type WorkshopBlockType =
  | "summary"
  | "evidence"
  | "preference"
  | "constraint"
  | "tag"
  | "inference"
  | "source"
  | "script_hook";

export interface WorkshopBlock {
  id: string;
  type: WorkshopBlockType;
  title: string;
  content: string;
  sourceAssetId: string;
  selected: boolean;
  weight?: number;
  confidence?: "high" | "medium" | "low";
  needsConfirmation?: boolean;
}

export type OutputStyle =
  | "compact_card"
  | "decision_advice"
  | "knowledge_explanation"
  | "script_reuse"
  | "evidence_strict";
```

#### `src/types/demo.ts`
全局演示控制类型定义。
```typescript
import { UiCubeModel } from "./cube";

export interface DemoScenario {
  id: string;
  title: string;
  subtitle: string;
  defaultContextId: string;
  heroCube: Partial<UiCubeModel>;
}
```

---

### 2. Demo 数据文件 (`src/data/`)

#### `src/data/fallbackCabinetAssets.ts`
陈列柜 Fallback 数据。
```typescript
import { AssetCube, CabinetShelf } from "../types/assets";

export const fallbackAssetCubes: Record<string, AssetCube> = {
  "asset_weekend_queue_avoidance": {
    id: "asset_weekend_queue_avoidance",
    title: "周末排队规避经验",
    shortSummary: "热门商圈周末排队时间参考及避峰建议",
    type: "experience",
    status: "formal",
    scenarioTags: ["本地生活", "效率", "排队"],
    sourceType: "demo_seed",
    usedCount: 3,
    lastUsedFor: "探店决策",
    createdAt: new Date().toISOString()
  },
  "asset_under_100_local_experience": {
    id: "asset_under_100_local_experience",
    title: "百元以内本地体验",
    shortSummary: "预算敏感型探店评价标准",
    type: "decision",
    status: "formal",
    scenarioTags: ["预算", "本地生活"],
    sourceType: "demo_seed",
    usedCount: 2,
    createdAt: new Date().toISOString()
  },
  "asset_bus_accessible_foods": {
    id: "asset_bus_accessible_foods",
    title: "公共交通便利餐饮",
    shortSummary: "无需自驾的餐饮选择",
    type: "experience",
    status: "formal",
    scenarioTags: ["交通", "本地生活"],
    sourceType: "demo_seed",
    usedCount: 1,
    createdAt: new Date().toISOString()
  }
};

export const fallbackCabinetShelves: CabinetShelf[] = [
  {
    id: "current_related",
    title: "当前相关",
    kind: "current_related",
    assetIds: [
      "asset_weekend_queue_avoidance",
      "asset_under_100_local_experience"
    ]
  },
  {
    id: "recent",
    title: "最近生成",
    kind: "recent",
    assetIds: []
  },
  {
    id: "draft",
    title: "待确认",
    kind: "draft",
    assetIds: []
  }
];
```

#### `src/data/demoScenarios.ts`
首页 3 个核心示例的数据桩（严格对应规格的探店、职场、财经）。
```typescript
import { DemoScenario } from "../types/demo";

export const demoScenarios: DemoScenario[] = [
  {
    id: "food_decision",
    title: "探店决策魔方",
    subtitle: "本地生活 / 低预算 / 少排队",
    defaultContextId: "ctx_low_budget_student",
    heroCube: {
      taskId: "task_demo_food_decision",
      assetId: "asset_demo_food_video",
      title: "这条探店视频适合我周六下午去吗？",
      state: "ready",
      phase: "final_form",
      referencedAssetIds: [
        "asset_weekend_queue_avoidance",
        "asset_under_100_local_experience",
        "asset_bus_accessible_foods"
      ],
      faces: {
        source: {
          id: "source",
          label: "原始内容",
          status: "ready",
          icon: "video-camera",
          accent: "neutral",
          summaryItems: ["探店视频", "本地生活", "约 2 分钟"],
          detailBlocks: []
        },
        primary_card: {
          id: "primary_card",
          label: "主重构卡",
          status: "ready",
          icon: "star",
          accent: "cyan",
          summaryItems: ["适合作为备选", "需确认价格和排队"],
          detailBlocks: []
        },
        related_assets: {
          id: "related_assets",
          label: "历史资产",
          status: "ready",
          icon: "database",
          accent: "blue",
          summaryItems: ["参考 3 个资产", "偏好 / 约束 / 经验"],
          detailBlocks: []
        },
        inferences: {
          id: "inferences",
          label: "推理待确认",
          status: "needs_confirmation",
          icon: "brain",
          accent: "amber",
          summaryItems: ["价格待确认", "排队风险"],
          needsConfirmation: true,
          detailBlocks: []
        },
        evidence: {
          id: "evidence",
          label: "证据链",
          status: "ready",
          icon: "shield-check",
          accent: "neutral",
          summaryItems: ["ASR", "历史资产", "用户偏好"],
          detailBlocks: []
        },
        snapshot: {
          id: "snapshot",
          label: "保存入柜",
          status: "ready",
          icon: "archive",
          accent: "neutral",
          summaryItems: ["可保存为决策资产"],
          detailBlocks: []
        }
      }
    }
  },
  {
    id: "workplace_skit",
    title: "职场短剧拆解魔方",
    subtitle: "剧情结构 / 广告植入 / 脚本复用",
    defaultContextId: "ctx_creator",
    heroCube: {
        taskId: "task_demo_workplace_skit",
        title: "爆款短剧脚本拆解",
        state: "input_ready",
        phase: "source_resolution"
    }
  },
  {
    id: "finance_knowledge",
    title: "财经知识解析魔方",
    subtitle: "观点摘要 / 证据链 / 风险提示",
    defaultContextId: "ctx_knowledge_worker",
    heroCube: {
        taskId: "task_demo_finance",
        title: "美联储降息视频关键结论",
        state: "input_ready",
        phase: "source_resolution"
    }
  }
];
```

---

### 3. 全局状态管理 (`src/store/`)

#### `src/store/useAppStore.ts`
应用级入口与演示环境控制。
```typescript
import { create } from "zustand";

export type AppMode = "current" | "cabinet" | "workshop";
export type DemoMode = "prefer_backend" | "force_fallback";
export type AnimationSpeed = "normal" | "fast" | "skip";

interface AppState {
  currentMode: AppMode;
  demoMode: DemoMode;
  animationSpeed: AnimationSpeed;
  currentDemoContext: string;
  apiBaseUrl: string;
  
  setMode: (mode: AppMode) => void;
  setDemoMode: (mode: DemoMode) => void;
  setAnimationSpeed: (speed: AnimationSpeed) => void;
  setDemoContext: (contextId: string) => void;
  setApiBaseUrl: (url: string) => void;
}

export const useAppStore = create<AppState>((set) => ({
  currentMode: "current",
  demoMode: "prefer_backend",
  animationSpeed: "normal",
  currentDemoContext: "ctx_low_budget_student",
  apiBaseUrl: "http://localhost:8080",

  setMode: (mode) => set({ currentMode: mode }),
  setDemoMode: (mode) => set({ demoMode: mode }),
  setAnimationSpeed: (speed) => set({ animationSpeed: speed }),
  setDemoContext: (contextId) => set({ currentDemoContext: contextId }),
  setApiBaseUrl: (url) => set({ apiBaseUrl: url }),
}));
```

#### `src/store/useCubeStore.ts`
当前主内容魔方及其渲染与动画状态。
```typescript
import { create } from "zustand";
import { UiCubeModel, CubeFaceId } from "../types/cube";
import { CubeAnimationPhase } from "../types/api";

type CubeInteractionState = "idle" | "dragging" | "animating";

interface CubeStore {
  currentCubeView: UiCubeModel | null;
  selectedFaceId: CubeFaceId | null;
  cubeInteractionState: CubeInteractionState;
  isAnimating: boolean;
  narrativeStage: string;
  processEvents: string[];

  setCurrentCubeView: (cube: UiCubeModel | null) => void;
  updateCubePhase: (phase: CubeAnimationPhase) => void;
  setSelectedFaceId: (faceId: CubeFaceId | null) => void;
  setInteractionState: (state: CubeInteractionState) => void;
  setIsAnimating: (isAnimating: boolean) => void;
  setNarrativeStage: (stage: string) => void;
  addProcessEvent: (event: string) => void;
  resetCubeStore: () => void;
}

export const useCubeStore = create<CubeStore>((set) => ({
  currentCubeView: null,
  selectedFaceId: null,
  cubeInteractionState: "idle",
  isAnimating: false,
  narrativeStage: "idle",
  processEvents: [],

  setCurrentCubeView: (cube) => set({ currentCubeView: cube }),
  updateCubePhase: (phase) => 
    set((state) => ({
      currentCubeView: state.currentCubeView 
        ? { ...state.currentCubeView, phase } 
        : null
    })),
  setSelectedFaceId: (faceId) => set({ selectedFaceId: faceId }),
  setInteractionState: (state) => set({ cubeInteractionState: state }),
  setIsAnimating: (isAnimating) => set({ isAnimating }),
  setNarrativeStage: (stage) => set({ narrativeStage: stage }),
  addProcessEvent: (event) => 
    set((state) => ({ processEvents: [...state.processEvents, event] })),
  resetCubeStore: () => set({
    currentCubeView: null,
    selectedFaceId: null,
    cubeInteractionState: "idle",
    isAnimating: false,
    narrativeStage: "idle",
    processEvents: []
  })
}));
```

#### `src/store/useCabinetStore.ts`
陈列柜资产状态管理（需响应持久化需求）。
```typescript
import { create } from "zustand";
import { AssetCube, CabinetShelf } from "../types/assets";
import { fallbackAssetCubes, fallbackCabinetShelves } from "../data/fallbackCabinetAssets";

type CabinetState = "collapsed" | "half" | "expanded";
type RelationPreviewState = "hidden" | "first_order" | "global";

interface CabinetStore {
  cabinetState: CabinetState;
  assets: Record<string, AssetCube>;
  shelves: CabinetShelf[];
  selectedAssetIds: string[];
  previewAssetId: string | null;
  searchQuery: string;
  filterMode: string;
  relationPreviewState: RelationPreviewState;

  setCabinetState: (state: CabinetState) => void;
  setAssets: (assets: Record<string, AssetCube>) => void;
  addAsset: (asset: AssetCube) => void;
  setShelves: (shelves: CabinetShelf[]) => void;
  toggleSelectedAsset: (assetId: string) => void;
  clearSelectedAssets: () => void;
  setPreviewAssetId: (assetId: string | null) => void;
  setSearchQuery: (query: string) => void;
  setFilterMode: (mode: string) => void;
  setRelationPreviewState: (state: RelationPreviewState) => void;
}

export const useCabinetStore = create<CabinetStore>((set) => ({
  cabinetState: "collapsed",
  assets: fallbackAssetCubes, // 默认注入 fallback
  shelves: fallbackCabinetShelves,
  selectedAssetIds: [],
  previewAssetId: null,
  searchQuery: "",
  filterMode: "all",
  relationPreviewState: "hidden",

  setCabinetState: (state) => set({ cabinetState: state }),
  setAssets: (assets) => set({ assets }),
  addAsset: (asset) => 
    set((state) => ({ assets: { ...state.assets, [asset.id]: asset } })),
  setShelves: (shelves) => set({ shelves }),
  toggleSelectedAsset: (assetId) => 
    set((state) => {
      const isSelected = state.selectedAssetIds.includes(assetId);
      return {
        selectedAssetIds: isSelected 
          ? state.selectedAssetIds.filter(id => id !== assetId)
          : [...state.selectedAssetIds, assetId]
      };
    }),
  clearSelectedAssets: () => set({ selectedAssetIds: [] }),
  setPreviewAssetId: (assetId) => set({ previewAssetId: assetId }),
  setSearchQuery: (query) => set({ searchQuery: query }),
  setFilterMode: (mode) => set({ filterMode: mode }),
  setRelationPreviewState: (state) => set({ relationPreviewState: state })
}));
```

#### `src/store/useWorkshopStore.ts`
魔方工坊组装逻辑状态管理。
```typescript
import { create } from "zustand";
import { WorkshopBlock, OutputStyle } from "../types/workshop";
import { UiCubeModel } from "../types/cube";

type WorkshopStage = "idle" | "disassembling" | "recombining" | "result";

interface WorkshopStore {
  selectedAssets: string[];
  semanticBlocks: WorkshopBlock[];
  selectedBlockIds: string[];
  recombinationTarget: string;
  outputStyle: OutputStyle;
  workshopStage: WorkshopStage;
  resultCube: UiCubeModel | null;

  setSelectedAssets: (assetIds: string[]) => void;
  setSemanticBlocks: (blocks: WorkshopBlock[]) => void;
  toggleSelectedBlock: (blockId: string) => void;
  setRecombinationTarget: (target: string) => void;
  setOutputStyle: (style: OutputStyle) => void;
  setWorkshopStage: (stage: WorkshopStage) => void;
  setResultCube: (cube: UiCubeModel | null) => void;
  resetWorkshop: () => void;
}

export const useWorkshopStore = create<WorkshopStore>((set) => ({
  selectedAssets: [],
  semanticBlocks: [],
  selectedBlockIds: [],
  recombinationTarget: "",
  outputStyle: "compact_card",
  workshopStage: "idle",
  resultCube: null,

  setSelectedAssets: (assetIds) => set({ selectedAssets: assetIds }),
  setSemanticBlocks: (blocks) => set({ semanticBlocks: blocks }),
  toggleSelectedBlock: (blockId) => 
    set((state) => {
      const isSelected = state.selectedBlockIds.includes(blockId);
      return {
        selectedBlockIds: isSelected
          ? state.selectedBlockIds.filter(id => id !== blockId)
          : [...state.selectedBlockIds, blockId]
      };
    }),
  setRecombinationTarget: (target) => set({ recombinationTarget: target }),
  setOutputStyle: (style) => set({ outputStyle: style }),
  setWorkshopStage: (stage) => set({ workshopStage: stage }),
  setResultCube: (cube) => set({ resultCube: cube }),
  resetWorkshop: () => set({
    selectedAssets: [],
    semanticBlocks: [],
    selectedBlockIds: [],
    recombinationTarget: "",
    outputStyle: "compact_card",
    workshopStage: "idle",
    resultCube: null
  })
}));
```

---

第一部分的强类型定义、数据约束和核心状态树已彻底建设完毕，严格遵守了不允许使用散落字符串状态和分离数据层的规范。

如果你准备好进入下一步，**请告知**，我将为你接着输出 AppShell 组件骨架、Tailwind 配置文件以及主页面布局系统（包括导航、左右面板与输入条）。