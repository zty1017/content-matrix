收到！接下来为你输出 **AppShell 组件骨架、全局样式配置以及主页面布局系统**。

这一步会把 Content Matrix 的“浅色冷调、玻璃/琉璃质感”视觉定调，并把我们在 `AppStore` 里定义的路由模式（`current` | `cabinet` | `workshop`）用 UI 串联起来。

---

### 1. 样式与全局配置

#### `src/index.css`
全局样式，包含 Tailwind 指令和符合规格的“浅色冷调、玻璃质感”底层基调。
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-slate-50 text-slate-800 overflow-hidden select-none font-sans;
    /* 极简冷色渐变背景，避免赛博深色 */
    background: radial-gradient(circle at 50% -20%, #e0f2fe 0%, #f8fafc 40%, #f1f5f9 100%);
  }
}

@layer utilities {
  /* 核心玻璃拟物组件库 */
  .glass-panel {
    @apply bg-white/40 backdrop-blur-md border border-white/60 shadow-[0_4px_30px_rgba(0,0,0,0.03)];
  }
  
  .glass-button {
    @apply bg-white/50 hover:bg-white/80 backdrop-blur-sm border border-white/50 shadow-sm transition-all duration-300;
  }

  .glass-button-active {
    @apply bg-blue-500/10 border-blue-400/50 text-blue-700 shadow-[inset_0_0_10px_rgba(59,130,246,0.1)];
  }

  .glass-pill {
    @apply bg-white/60 backdrop-blur-md border border-white/80 rounded-full shadow-sm;
  }
}
```

#### `src/main.tsx`
标准 Vite React 入口文件。
```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';
import './index.css';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

---

### 2. 布局组件层 (`src/components/layout/`)

#### `src/components/layout/TopNav.tsx`
顶部导航，包含三大模式切换与右侧的 Demo 控制面板入口。
```tsx
import React from 'react';
import { useAppStore } from '../../store/useAppStore';

export const TopNav: React.FC = () => {
  const { currentMode, setMode, demoMode, setDemoMode } = useAppStore();

  const navItems = [
    { id: 'current', label: '当前魔方' },
    { id: 'cabinet', label: '资产陈列柜' },
    { id: 'workshop', label: '魔方工坊' },
  ] as const;

  return (
    <header className="absolute top-0 left-0 right-0 h-16 z-50 flex items-center justify-between px-6 glass-panel border-b border-white/40">
      {/* Logo & Brand */}
      <div className="flex items-center gap-3">
        <div className="w-8 h-8 rounded bg-gradient-to-br from-cyan-400 to-blue-500 flex items-center justify-center shadow-inner text-white font-bold">
          CM
        </div>
        <span className="font-semibold text-slate-700 tracking-wide">Content Matrix</span>
      </div>

      {/* Main Navigation */}
      <nav className="flex items-center gap-2 glass-pill p-1">
        {navItems.map((item) => (
          <button
            key={item.id}
            onClick={() => setMode(item.id)}
            className={`px-5 py-1.5 rounded-full text-sm font-medium transition-all ${
              currentMode === item.id
                ? 'bg-white text-blue-600 shadow-sm'
                : 'text-slate-500 hover:text-slate-700 hover:bg-white/50'
            }`}
          >
            {item.label}
          </button>
        ))}
      </nav>

      {/* Demo Control Panel (Mini Version) */}
      <div className="flex items-center gap-3 text-sm">
        <div className="flex items-center gap-2 bg-slate-100/50 px-3 py-1.5 rounded-full border border-slate-200/50">
          <span className="w-2 h-2 rounded-full bg-emerald-400 animate-pulse"></span>
          <select 
            value={demoMode} 
            onChange={(e) => setDemoMode(e.target.value as any)}
            className="bg-transparent border-none outline-none text-slate-600 font-medium cursor-pointer"
          >
            <option value="prefer_backend">优先后端 API</option>
            <option value="force_fallback">强制 Fallback</option>
          </select>
        </div>
      </div>
    </header>
  );
};
```

#### `src/components/layout/BottomInputBar.tsx`
底部的悬浮输入条，采用组件组合模式。
```tsx
import React, { useState } from 'react';

export const BottomInputBar: React.FC = () => {
  const [inputValue, setInputValue] = useState('');

  const handleReconstruct = () => {
    if (!inputValue.trim()) return;
    console.log("Start reconstruction with:", inputValue);
    // 后续对接打乱与解析逻辑
  };

  return (
    <div className="absolute bottom-8 left-1/2 -translate-x-1/2 z-40 w-full max-w-2xl">
      <div className="glass-panel rounded-2xl p-2 flex items-center gap-2 shadow-lg shadow-blue-900/5">
        <div className="flex-1 px-4 py-2">
          <input
            type="text"
            value={inputValue}
            onChange={(e) => setInputValue(e.target.value)}
            placeholder="粘贴抖音链接 / 输入目标 / 拖入本地视频..."
            className="w-full bg-transparent outline-none text-slate-700 placeholder:text-slate-400"
            onKeyDown={(e) => e.key === 'Enter' && handleReconstruct()}
          />
        </div>
        <div className="w-px h-6 bg-slate-300/50 mx-2"></div>
        <button 
          onClick={handleReconstruct}
          className="bg-blue-600 hover:bg-blue-500 text-white px-6 py-2 rounded-xl text-sm font-medium transition-colors shadow-sm shadow-blue-500/30"
        >
          开始重构
        </button>
      </div>
    </div>
  );
};
```

#### `src/components/layout/NarrativeStatusBar.tsx`
顶部正下方的阶段胶囊（叙事型状态栏）。
```tsx
import React from 'react';
import { useCubeStore } from '../../store/useCubeStore';

export const NarrativeStatusBar: React.FC = () => {
  const { narrativeStage } = useCubeStore();

  const stages = [
    { id: 'source_resolution', label: '来源识别' },
    { id: 'content_reconstruction', label: '内容拆解' },
    { id: 'matrix_linking', label: '资产增强' },
    { id: 'final_form', label: '魔方成型' },
  ];

  // Dummy active index for UI showcase
  const activeIndex = stages.findIndex(s => s.id === narrativeStage) >= 0 ? stages.findIndex(s => s.id === narrativeStage) : 0;

  return (
    <div className="absolute top-20 left-1/2 -translate-x-1/2 z-40 flex items-center gap-2">
      {stages.map((stage, index) => {
        const isPast = index < activeIndex;
        const isActive = index === activeIndex;
        
        return (
          <React.Fragment key={stage.id}>
            <div className={`glass-pill px-4 py-1.5 text-xs font-medium transition-all duration-500 flex items-center gap-2 ${
              isActive ? 'text-blue-600 bg-white shadow-[0_0_15px_rgba(59,130,246,0.15)] ring-1 ring-blue-400/30' : 
              isPast ? 'text-slate-500' : 'text-slate-400 bg-white/20'
            }`}>
              {isPast && <span className="text-emerald-500">✓</span>}
              {isActive && <span className="w-1.5 h-1.5 rounded-full bg-blue-500 animate-pulse"></span>}
              {stage.label}
            </div>
            {index < stages.length - 1 && (
              <div className={`w-4 h-[1px] transition-colors duration-500 ${isPast ? 'bg-blue-300' : 'bg-slate-300'}`} />
            )}
          </React.Fragment>
        );
      })}
    </div>
  );
};
```

#### `src/components/layout/AppShell.tsx`
最外层大骨架容器，按工作区结构分离 `当前魔方区` / `工坊区`。
```tsx
import React from 'react';
import { useAppStore } from '../../store/useAppStore';
import { TopNav } from './TopNav';
import { BottomInputBar } from './BottomInputBar';
import { NarrativeStatusBar } from './NarrativeStatusBar';

// 临时占位组件，下一次对话实现真实组件
const CubeWorkspacePlaceholder = () => (
  <div className="absolute inset-0 flex items-center justify-center flex-col z-0">
    <div className="w-[400px] h-[400px] border border-blue-200 border-dashed rounded-xl flex items-center justify-center bg-blue-50/30 backdrop-blur-sm">
      <p className="text-blue-500 font-medium">3D Content Matrix (R3F Canvas)</p>
    </div>
  </div>
);

const CabinetPlaceholder = () => (
  <div className="absolute left-0 top-16 bottom-0 w-80 glass-panel border-l-0 rounded-r-2xl z-30 transform transition-transform">
    <div className="p-4"><p className="text-slate-500 text-sm font-medium">资产陈列柜 (Cabinet)</p></div>
  </div>
);

const RightPanelPlaceholder = () => (
  <div className="absolute right-6 top-24 bottom-24 w-80 glass-panel rounded-2xl z-30">
    <div className="p-4"><p className="text-slate-500 text-sm font-medium">六面详情面板 (Panels)</p></div>
  </div>
);

const WorkshopPlaceholder = () => (
  <div className="absolute inset-0 top-16 bg-slate-50/90 backdrop-blur-xl z-20 flex items-center justify-center">
    <h2 className="text-slate-400 text-2xl font-bold tracking-wider">魔方工坊 (Workshop) 沉浸视图</h2>
  </div>
);

export const AppShell: React.FC = () => {
  const { currentMode } = useAppStore();

  return (
    <div className="relative w-screen h-screen overflow-hidden">
      <TopNav />
      
      {/* 核心工作区 (当前魔方 & 资产柜模式) */}
      {(currentMode === 'current' || currentMode === 'cabinet') && (
        <>
          <NarrativeStatusBar />
          <CabinetPlaceholder />
          <CubeWorkspacePlaceholder />
          <RightPanelPlaceholder />
          <BottomInputBar />
        </>
      )}

      {/* 工坊模式视图 */}
      {currentMode === 'workshop' && (
        <WorkshopPlaceholder />
      )}
    </div>
  );
};
```

---

### 3. 主应用入口

#### `src/App.tsx`
挂载 AppShell。
```tsx
import React from 'react';
import { AppShell } from './components/layout/AppShell';

const App: React.FC = () => {
  return (
    <AppShell />
  );
};

export default App;
```

---

### 当前进展总结
目前已经完成：
1. **Typescript + Zustand 数据流架构** 
2. **全局 CSS 样式体系**（玻璃质感组件）
3. **AppShell 空间化响应式布局**（导航、输入、状态栏及占位区）

接下来的**最核心部分是 3D 魔方实现（React Three Fiber + Drei）**。我将为你输出：
1. `src/components/cube/MatrixCube.tsx`：标准 3x3x3 分层结构。
2. 魔方的层旋转系统（U层、R层旋转等真实还原）。
3. 整体拖拽交互与点击识别。

如果你准备好了，**请回复继续**，我将进入 3D 核心的开发！